{
    "name": "socket_test",
    "version": "1.0.0",
    "description": "socket_test",
    "main": "server.js",
    "scripts": {
        "start": "node server.js" //*
        "test": "echo \"Error: no test specified\" && exit 1"
      },

    "dependencies": {
        "express": "^4.15.2",
        "socket.io": "^2.2.0"
    }
}
